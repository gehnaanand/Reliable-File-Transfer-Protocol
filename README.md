# FTP-Reliable

This project is a simple client-server program that establishes a UDP connection and implements file transfer by incorporating features of TCP i.e. making it reliable connection (take into account for packet loss and retransmit).
